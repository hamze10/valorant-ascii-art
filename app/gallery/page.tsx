import GalleryGrid from "./GalleryGrid";

export const metadata = {
    title: 'Valorant ASCII Art Gallery — Crosshairs, Symbols & Copy-Pastas',
    description: 'Browse ready-to-copy Valorant ASCII art: crosshairs, rank symbols, ability icons and GGWP copy-pastas. One-click copy for in-game chat.',
};

export default function GalleryPage() {
    return <GalleryGrid />;
}